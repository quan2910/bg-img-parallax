import "./globals.css";

export const metadata = {
  title: "Bg Image Parallax",
  description: "Bg Image Parallax",
};

const Layout = ({ children }) => (
  <html lang="en">
    <body>{children}</body>
  </html>
);

export default Layout;
